# frozen_string_literal: true

source 'https://rubygems.org'

# Specify your gem's dependencies in bioruby-mcp-server.gemspec
gemspec

gem 'minitest', '~> 5.0'
gem 'rake', '~> 13.0'
gem 'rubocop', '~> 1.21'

# Add JSON RPC handler dependency for MCP SDK
gem 'json_rpc_handler', '~> 0.1'
